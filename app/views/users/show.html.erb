<p id="notice"><%= notice %></p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>

<p>
  <strong>Instant payment allowed:</strong>
  <%= @user.instant_payment_allowed %>
</p>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', (current_user.admin? ? users_path : root_path) %>
